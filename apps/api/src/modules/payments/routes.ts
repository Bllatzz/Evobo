import type { FastifyInstance } from "fastify";

/** Scaffold stub — real routes land when this module's roadmap phase starts. */
export async function paymentsRoutes(_app: FastifyInstance) {
  // TODO: implement payments routes
}
