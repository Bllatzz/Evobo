# Public by design — the anon key is safe in a frontend bundle (RLS is what
# actually protects data, not this key). Never put the service_role key here.
VITE_SUPABASE_URL="http://localhost:54321"
VITE_SUPABASE_ANON_KEY=""

VITE_API_URL="http://localhost:3000"
